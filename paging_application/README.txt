Contents of this directory:

	· src: folder containing the proportioned document.txt, a Makefile to compile my program, generate a jar and execute it,
		the program PageFormatter.java itself, the manifest file for the jar and a README with the instructions to execute the program.
	· out: folder where the compiled .java (PageFormatter.class) is generated.
	· exe: folder where the executable .jar (PageFormatter.jar) is generated
